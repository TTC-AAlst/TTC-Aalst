# Toog-beschikbaarheid Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Spelers geven per clubthuisdag aan of ze de toog kunnen doen; een admin duidt per dag één verantwoordelijke aan, die ook in de Excel-export verschijnt.

**Architecture:** Eén nieuwe tabel `Toog` (`Date`, `PlayerId`, `Assigned`). Clubthuisdagen worden afgeleid uit `Match` (`HomeTeamId != null`), niet opgeslagen. Een nieuwe `ToogService` + `ToogController` bedienen twee schermen: een extra kolom in `PlayerLineup.tsx` en een nieuwe admin-tab `AdminToog.tsx`.

**Tech Stack:** .NET 10 / EF Core (MySql.EntityFrameworkCore) / xUnit + Testcontainers; React 18 + Redux Toolkit + Vite / Vitest + React Testing Library.

**Spec:** `docs/superpowers/specs/2026-09-14-toog-design.md`

---

## Achtergrond die je nodig hebt

- Backend-services staan in `backend/src/Ttc.DataAccess/Services/`, worden geregistreerd in
  `backend/src/Ttc.DataAccess/GlobalBackendConfiguration.cs`, en krijgen `ITtcDbContext` +
  `IUserProvider` geïnjecteerd.
- `IUserProvider.PlayerId` is de ingelogde speler (uit de JWT). Nooit een `playerId` uit de
  request body vertrouwen.
- "Admin" = `PlayerEntity.Security != PlayerAccess.Player` (`Board`, `Dev` of `System`).
- Controllers geven `new StatusCodeResult(StatusCodes.Status403Forbidden)` terug bij een
  verboden actie; zie `MatchesController.EditMatchPlayers`.
- Integratietests erven van `IntegrationTestBase`, draaien tegen een echte MySQL via
  Testcontainers en maken hun eigen client met een JWT via `UserProvider.GenerateJwtToken`.
  Op Windows: `TESTCONTAINERS_RYUK_DISABLED=true`.
- De frontend praat met de backend via `frontend/src/utils/httpClient.ts`, dat enkel `get` en
  `post` kent en zelf `/api` voorvoegt.
- Frontend package manager is **bun**, niet npm.

## Bestandsoverzicht

| Bestand | Verantwoordelijkheid |
|---------|----------------------|
| `backend/src/Ttc.DataEntities/ToogEntity.cs` | nieuw — EF-entity voor tabel `Toog` |
| `backend/src/Ttc.DataEntities/Core/ITtcDbContext.cs` | wijzig — `DbSet<ToogEntity> Toog` |
| `backend/src/Ttc.DataAccess/TtcDbContext.cs` | wijzig — DbSet + unieke index in `OnModelCreating` |
| `backend/src/Ttc.DataAccess/Migrations/*_Toog.cs` | nieuw — gegenereerde migratie |
| `backend/src/Ttc.Model/Toog/ToogModels.cs` | nieuw — DTO's voor beide schermen |
| `backend/src/Ttc.DataAccess/Services/ToogService.cs` | nieuw — thuisdag-afleiding, beschikbaarheid, aanduiding |
| `backend/src/Ttc.DataAccess/GlobalBackendConfiguration.cs` | wijzig — `AddScoped<ToogService>()` |
| `backend/src/Ttc.WebApi/Controllers/ToogController.cs` | nieuw — 4 endpoints |
| `backend/src/Ttc.UnitTests/Integration/ToogControllerTests.cs` | nieuw — integratietests |
| `backend/src/Ttc.DataAccess/Utilities/Excel/TeamsExcelCreator.cs` | wijzig — kolom "Toog" |
| `backend/src/Ttc.DataAccess/Utilities/Excel/ExcelExportResources.resx` + `.Designer.cs` | wijzig — resource `MatchToog` |
| `backend/src/Ttc.DataAccess/Services/TeamService.cs` | wijzig — toog-rijen meegeven aan de Excel-builder |
| `frontend/src/models/model-interfaces.ts` | wijzig — `IToogDay`, `IToogAdminDay` |
| `frontend/src/reducers/toogReducer.ts` | nieuw — thunks + slice |
| `frontend/src/store.ts` + `frontend/src/utils/test-utils.tsx` | wijzig — reducer registreren |
| `frontend/src/utils/locales-nl.ts` | wijzig — teksten |
| `frontend/src/components/users/PlayerLineup.tsx` | wijzig — toog-kolom en toog-only rijen |
| `frontend/src/components/users/Profile.tsx` | wijzig — `showToog` meegeven |
| `frontend/src/components/users/spec/PlayerLineupToogSpec.tsx` | nieuw |
| `frontend/src/components/admin/AdminToog.tsx` | nieuw — admin-tab |
| `frontend/src/components/admin/Admin.tsx` | wijzig — tab toevoegen |
| `frontend/src/components/admin/spec/AdminToogSpec.tsx` | nieuw |

---

## Task 1: Entity, DbContext en migratie

**Files:**
- Create: `backend/src/Ttc.DataEntities/ToogEntity.cs`
- Modify: `backend/src/Ttc.DataEntities/Core/ITtcDbContext.cs`
- Modify: `backend/src/Ttc.DataAccess/TtcDbContext.cs`
- Create: `backend/src/Ttc.DataAccess/Migrations/<timestamp>_Toog.cs` (gegenereerd)

- [ ] **Step 1: Schrijf de entity**

`backend/src/Ttc.DataEntities/ToogEntity.cs`:

```csharp
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;
using Microsoft.EntityFrameworkCore;

namespace Ttc.DataEntities;

[Table("Toog")]
[Index(nameof(Date), nameof(PlayerId), IsUnique = true)]
public class ToogEntity
{
    [Key]
    public int Id { get; set; }

    /// <summary>
    /// Clubthuisdag, altijd om middernacht: een toogbeurt geldt voor de hele dag,
    /// ook als er die avond meerdere ploegen thuis spelen.
    /// </summary>
    [Column(TypeName = "date")]
    public DateTime Date { get; set; }

    [ForeignKey("PlayerId")]
    public PlayerEntity? Player { get; set; }
    public int PlayerId { get; set; }

    public bool Assigned { get; set; }

    /// <summary>
    /// False wanneer het bestuur deze rij aanmaakte voor iemand die zich niet opgaf:
    /// zo'n rij verdwijnt weer zodra de aanduiding naar iemand anders gaat, terwijl de rij van
    /// een vrijwilliger blijft staan.
    /// </summary>
    public bool Volunteered { get; set; }

    public override string ToString() => $"Date={Date:d}, PlayerId={PlayerId}, Assigned={Assigned}";
}
```

- [ ] **Step 2: Voeg de DbSet toe aan de interface**

In `backend/src/Ttc.DataEntities/Core/ITtcDbContext.cs`, na de regel
`DbSet<EventEntity> Events { get; set; }`:

```csharp
    DbSet<ToogEntity> Toog { get; set; }
```

- [ ] **Step 3: Voeg de DbSet toe aan de context**

In `backend/src/Ttc.DataAccess/TtcDbContext.cs`, in het `#region DbSets`-blok na
`public DbSet<EventEntity> Events { get; set; } = null!;`:

```csharp
    public DbSet<ToogEntity> Toog { get; set; } = null!;
```

- [ ] **Step 4: Genereer de migratie**

```bash
cd backend
dotnet ef migrations add Toog -p src/Ttc.DataAccess -s src/Ttc.WebApi
```

Verwacht: twee nieuwe bestanden in `src/Ttc.DataAccess/Migrations/` plus een bijgewerkte
`TtcDbContextModelSnapshot.cs`.

- [ ] **Step 5: Controleer de migratie**

Open het gegenereerde `*_Toog.cs`. `Up()` moet `CreateTable("Toog", ...)` bevatten met kolommen
`Id`, `Date` (type `date`), `PlayerId`, `Assigned`, `Volunteered`, en een `CreateIndex` met `unique: true` op
(`Date`, `PlayerId`). Staat er geen unieke index, voeg ze handmatig toe:

```csharp
migrationBuilder.CreateIndex(
    name: "IX_Toog_Date_PlayerId",
    table: "Toog",
    columns: new[] { "Date", "PlayerId" },
    unique: true);
```

- [ ] **Step 6: Build**

```bash
cd backend && dotnet build Ttc.slnx
```

Verwacht: `Build succeeded`, 0 warnings.

- [ ] **Step 7: Commit**

```bash
git add backend/src/Ttc.DataEntities/ToogEntity.cs backend/src/Ttc.DataEntities/Core/ITtcDbContext.cs backend/src/Ttc.DataAccess/TtcDbContext.cs backend/src/Ttc.DataAccess/Migrations
git commit -m "Add Toog table for bar duty availability"
```

---

## Task 2: DTO's en ToogService-skelet

**Files:**
- Create: `backend/src/Ttc.Model/Toog/ToogModels.cs`
- Create: `backend/src/Ttc.DataAccess/Services/ToogService.cs`
- Modify: `backend/src/Ttc.DataAccess/GlobalBackendConfiguration.cs`

Deze taak heeft geen eigen test: Task 3 test het gedrag via de controller. Hier zetten we enkel
de types neer die Task 3 en 4 nodig hebben.

- [ ] **Step 1: Schrijf de DTO's**

`backend/src/Ttc.Model/Toog/ToogModels.cs`:

```csharp
namespace Ttc.Model.Toog;

/// <summary>Eén clubthuisdag, gezien door de ingelogde speler.</summary>
public class ToogDay
{
    public DateTime Date { get; set; }
    public int[] HomeTeamIds { get; set; } = [];
    public bool Available { get; set; }
    public bool Assigned { get; set; }
}

/// <summary>Eén clubthuisdag, gezien door het bestuur.</summary>
public class ToogAdminDay
{
    public DateTime Date { get; set; }
    public int[] HomeTeamIds { get; set; } = [];
    public int[] AvailablePlayerIds { get; set; } = [];
    public int? AssignedPlayerId { get; set; }
}

public class ToogAvailabilityRequest
{
    public DateTime Date { get; set; }
    public bool Available { get; set; }
}

public class ToogAssignRequest
{
    public DateTime Date { get; set; }
    public int? PlayerId { get; set; }
}
```

- [ ] **Step 2: Schrijf de service met de thuisdag-afleiding**

`backend/src/Ttc.DataAccess/Services/ToogService.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Ttc.DataEntities;
using Ttc.DataEntities.Core;
using Ttc.Model.Toog;

namespace Ttc.DataAccess.Services;

public class ToogService
{
    private readonly ITtcDbContext _context;
    private readonly IUserProvider _userProvider;

    public ToogService(ITtcDbContext context, IUserProvider userProvider)
    {
        _context = context;
        _userProvider = userProvider;
    }

    /// <summary>
    /// Komende dagen waarop minstens één van onze ploegen thuis speelt, met de betrokken ploegen.
    /// Afgeleid uit de kalender: een toogbeurt heeft geen eigen kalenderrecord.
    /// </summary>
    private async Task<Dictionary<DateTime, int[]>> GetHomeDays()
    {
        int currentFrenoySeason = _context.CurrentFrenoySeason;
        var today = TtcDbContext.GetCurrentBelgianDateTime().Date;

        var matches = await _context.Matches
            .Where(x => x.FrenoySeason == currentFrenoySeason)
            .Where(x => x.ShouldBePlayed)
            .Where(x => x.HomeTeamId != null)
            .Where(x => x.Date >= today)
            .Select(x => new { x.Date, HomeTeamId = x.HomeTeamId!.Value })
            .ToArrayAsync();

        return matches
            .GroupBy(x => x.Date.Date)
            .OrderBy(x => x.Key)
            .ToDictionary(x => x.Key, x => x.Select(m => m.HomeTeamId).Distinct().OrderBy(id => id).ToArray());
    }

    public async Task<bool> IsBoardMember()
    {
        int? playerId = _userProvider.PlayerId;
        if (!playerId.HasValue)
        {
            return false;
        }

        var player = await _context.Players.FindAsync(playerId.Value);
        return player != null && player.Security != PlayerAccess.Player;
    }
}
```

- [ ] **Step 3: Registreer de service**

In `backend/src/Ttc.DataAccess/GlobalBackendConfiguration.cs`, in `ConfigureServices`, na
`services.AddScoped<PlayerService>();`:

```csharp
        services.AddScoped<ToogService>();
```

- [ ] **Step 4: Build**

```bash
cd backend && dotnet build Ttc.slnx
```

Verwacht: `Build succeeded`, 0 warnings.

- [ ] **Step 5: Commit**

```bash
git add backend/src/Ttc.Model/Toog backend/src/Ttc.DataAccess/Services/ToogService.cs backend/src/Ttc.DataAccess/GlobalBackendConfiguration.cs
git commit -m "Add ToogService with club home day derivation"
```

---

## Task 3: Speler-endpoints (`/api/toog/mine`)

**Files:**
- Modify: `backend/src/Ttc.DataAccess/Services/ToogService.cs`
- Create: `backend/src/Ttc.WebApi/Controllers/ToogController.cs`
- Create: `backend/src/Ttc.UnitTests/Integration/ToogControllerTests.cs`

- [ ] **Step 1: Schrijf de falende tests**

`backend/src/Ttc.UnitTests/Integration/ToogControllerTests.cs`:

```csharp
using System.Net;
using System.Net.Http.Headers;
using System.Net.Http.Json;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using Ttc.DataEntities;
using Ttc.Model;
using Ttc.Model.Players;
using Ttc.Model.Teams;
using Ttc.Model.Toog;
using Ttc.WebApi.Utilities.Auth;

namespace Ttc.UnitTests.Integration;

public class ToogControllerTests : IntegrationTestBase
{
    private const int TeamId = 940;
    private const int SecondTeamId = 941;
    private const int PlainPlayerId = 942;
    private const int BoardPlayerId = 943;
    private const int OtherPlayerId = 944;
    private const int HomeMatchId = 950;
    private const int SameDayHomeMatchId = 951;
    private const int AwayMatchId = 952;

    private static readonly DateTime HomeDay = DateTime.Today.AddDays(7).AddHours(20);
    private static readonly DateTime AwayDay = DateTime.Today.AddDays(9).AddHours(20);

    public ToogControllerTests(TtcWebApplicationFactory factory) : base(factory)
    {
    }

    public override async Task InitializeAsync()
    {
        await base.InitializeAsync();
        await using var context = GetDbContext();
        await context.Database.MigrateAsync();
        // The migrations annotate identity columns with Pomelo's "MySql:ValueGenerationStrategy", which
        // MySql.EntityFrameworkCore ignores, so a migrated database has no AUTO_INCREMENT anywhere.
        await context.Database.ExecuteSqlRawAsync("ALTER TABLE Toog MODIFY Id INT NOT NULL AUTO_INCREMENT");

        if (!await context.Parameters.AnyAsync())
        {
            context.Parameters.Add(new ParameterEntity { Key = "year", Value = "2024" });
            await context.SaveChangesAsync();
        }

        if (!await context.Teams.AnyAsync(x => x.Id == TeamId))
        {
            context.Teams.Add(new TeamEntity { Id = TeamId, Competition = Competition.Vttl, Year = 2024, TeamCode = "A" });
            context.Teams.Add(new TeamEntity { Id = SecondTeamId, Competition = Competition.Sporta, Year = 2024, TeamCode = "B" });
            context.Players.Add(NewPlayer(PlainPlayerId, "Plain", PlayerAccess.Player));
            context.Players.Add(NewPlayer(BoardPlayerId, "Board", PlayerAccess.Board));
            context.Players.Add(NewPlayer(OtherPlayerId, "Other", PlayerAccess.Player));
            context.Matches.Add(NewMatch(HomeMatchId, HomeDay, TeamId, isHome: true));
            context.Matches.Add(NewMatch(SameDayHomeMatchId, HomeDay.AddHours(-1), SecondTeamId, isHome: true));
            context.Matches.Add(NewMatch(AwayMatchId, AwayDay, TeamId, isHome: false));
            await context.SaveChangesAsync();
        }

        await using var cleanup = GetDbContext();
        cleanup.Toog.RemoveRange(await cleanup.Toog.ToArrayAsync());
        await cleanup.SaveChangesAsync();
    }

    private static PlayerEntity NewPlayer(int id, string alias, PlayerAccess access) => new()
    {
        Id = id,
        Alias = alias,
        FirstName = alias,
        LastName = "Tester",
        Security = access,
    };

    private static MatchEntity NewMatch(int id, DateTime date, int teamId, bool isHome) => new()
    {
        Id = id,
        Date = date,
        ShouldBePlayed = true,
        Week = 1,
        Competition = Competition.Vttl,
        FrenoySeason = 25,
        FrenoyDivisionId = 1,
        HomeTeamId = isHome ? teamId : null,
        HomeClubId = isHome ? Constants.OwnClubId : 2,
        HomeTeamCode = "A",
        AwayTeamId = isHome ? null : teamId,
        AwayClubId = isHome ? 2 : Constants.OwnClubId,
        AwayTeamCode = "B",
    };

    private HttpClient CreateClientFor(int playerId)
    {
        using var scope = Factory.Services.CreateScope();
        var userProvider = scope.ServiceProvider.GetRequiredService<UserProvider>();
        string token = userProvider.GenerateJwtToken(new User { PlayerId = playerId, Alias = "Tester", Teams = [TeamId] });

        var client = Factory.CreateClient();
        client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", token);
        return client;
    }

    [Fact]
    public async Task GetMine_ReturnsOneRowPerHomeDay()
    {
        using var client = CreateClientFor(PlainPlayerId);

        var days = await client.GetFromJsonAsync<ToogDay[]>("api/toog/mine");

        Assert.NotNull(days);
        var day = Assert.Single(days, x => x.Date.Date == HomeDay.Date);
        Assert.Equal([TeamId, SecondTeamId], day.HomeTeamIds.OrderBy(x => x));
        Assert.False(day.Available);
        Assert.DoesNotContain(days, x => x.Date.Date == AwayDay.Date);
    }

    [Fact]
    public async Task SetMine_Available_AddsARow()
    {
        using var client = CreateClientFor(PlainPlayerId);

        var response = await client.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = true });

        response.EnsureSuccessStatusCode();
        await using var context = GetDbContext();
        var row = Assert.Single(await context.Toog.Where(x => x.PlayerId == PlainPlayerId).ToArrayAsync());
        Assert.Equal(HomeDay.Date, row.Date);
        Assert.False(row.Assigned);
    }

    [Fact]
    public async Task SetMine_Unavailable_RemovesTheRow()
    {
        using var client = CreateClientFor(PlainPlayerId);
        (await client.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = true })).EnsureSuccessStatusCode();

        var response = await client.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = false });

        response.EnsureSuccessStatusCode();
        await using var context = GetDbContext();
        Assert.Empty(await context.Toog.Where(x => x.PlayerId == PlainPlayerId).ToArrayAsync());
    }

    [Fact]
    public async Task SetMine_OnADayWithoutHomeMatch_IsForbidden()
    {
        using var client = CreateClientFor(PlainPlayerId);

        var response = await client.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = AwayDay.Date, Available = true });

        Assert.Equal(HttpStatusCode.Forbidden, response.StatusCode);
        await using var context = GetDbContext();
        Assert.Empty(await context.Toog.ToArrayAsync());
    }
}
```

- [ ] **Step 2: Draai de tests en zie ze falen**

```bash
cd backend && dotnet test Ttc.slnx --filter FullyQualifiedName~ToogControllerTests
```

Verwacht: compileerfout of 4 gefaalde tests — `api/toog/mine` bestaat nog niet.

- [ ] **Step 3: Vul de service aan**

Voeg toe aan `backend/src/Ttc.DataAccess/Services/ToogService.cs`, onder `IsBoardMember`:

```csharp
    public async Task<ToogDay[]> GetMine()
    {
        int playerId = _userProvider.PlayerId ?? throw new InvalidOperationException("Not logged in");
        var homeDays = await GetHomeDays();
        var mine = await _context.Toog.Where(x => x.PlayerId == playerId).ToArrayAsync();

        return homeDays
            .Select(day => new ToogDay
            {
                Date = day.Key,
                HomeTeamIds = day.Value,
                Available = mine.Any(x => x.Date == day.Key),
                Assigned = mine.Any(x => x.Date == day.Key && x.Assigned),
            })
            .ToArray();
    }

    /// <summary>
    /// Mag de ingelogde speler zijn beschikbaarheid voor deze dag nog wijzigen?
    /// Nee zodra hij is aangeduid: dan moet het bestuur eerst iemand anders kiezen.
    /// </summary>
    public async Task<bool> MaySetMine(DateTime date, bool available)
    {
        int? playerId = _userProvider.PlayerId;
        if (!playerId.HasValue)
        {
            return false;
        }

        var day = date.Date;
        var homeDays = await GetHomeDays();
        if (!homeDays.ContainsKey(day))
        {
            return false;
        }

        if (available)
        {
            return true;
        }

        var existing = await _context.Toog.SingleOrDefaultAsync(x => x.PlayerId == playerId.Value && x.Date == day);
        return existing == null || !existing.Assigned;
    }

    public async Task<ToogDay[]> SetMine(DateTime date, bool available)
    {
        int playerId = _userProvider.PlayerId ?? throw new InvalidOperationException("Not logged in");
        var day = date.Date;
        var existing = await _context.Toog.SingleOrDefaultAsync(x => x.PlayerId == playerId && x.Date == day);

        if (available && existing == null)
        {
            _context.Toog.Add(new ToogEntity { Date = day, PlayerId = playerId, Volunteered = true });
        }
        else if (!available && existing != null)
        {
            _context.Toog.Remove(existing);
        }

        await _context.SaveChangesAsync();
        return await GetMine();
    }
```

- [ ] **Step 4: Schrijf de controller**

`backend/src/Ttc.WebApi/Controllers/ToogController.cs`:

```csharp
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Ttc.DataAccess.Services;
using Ttc.Model.Toog;

namespace Ttc.WebApi.Controllers;

[Authorize]
[Route("api/toog")]
public class ToogController
{
    private readonly ToogService _service;

    public ToogController(ToogService service)
    {
        _service = service;
    }

    [HttpGet("mine")]
    public async Task<ToogDay[]> GetMine()
    {
        return await _service.GetMine();
    }

    [HttpPost("mine")]
    public async Task<ActionResult<ToogDay[]>> SetMine([FromBody] ToogAvailabilityRequest request)
    {
        if (!await _service.MaySetMine(request.Date, request.Available))
        {
            return new StatusCodeResult(StatusCodes.Status403Forbidden);
        }

        return await _service.SetMine(request.Date, request.Available);
    }
}
```

- [ ] **Step 5: Draai de tests**

```bash
cd backend && dotnet test Ttc.slnx --filter FullyQualifiedName~ToogControllerTests
```

Verwacht: 4 passed.

- [ ] **Step 6: Commit**

```bash
git add backend/src/Ttc.DataAccess/Services/ToogService.cs backend/src/Ttc.WebApi/Controllers/ToogController.cs backend/src/Ttc.UnitTests/Integration/ToogControllerTests.cs
git commit -m "Let a player mark themselves available for bar duty"
```

---

## Task 4: Admin-endpoints (`/api/toog` en `/api/toog/assign`)

**Files:**
- Modify: `backend/src/Ttc.DataAccess/Services/ToogService.cs`
- Modify: `backend/src/Ttc.WebApi/Controllers/ToogController.cs`
- Modify: `backend/src/Ttc.UnitTests/Integration/ToogControllerTests.cs`

- [ ] **Step 1: Schrijf de falende tests**

Voeg onderaan in `ToogControllerTests` toe, vóór de afsluitende `}`:

```csharp
    [Fact]
    public async Task Get_AsPlainPlayer_IsForbidden()
    {
        using var client = CreateClientFor(PlainPlayerId);

        var response = await client.GetAsync("api/toog");

        Assert.Equal(HttpStatusCode.Forbidden, response.StatusCode);
    }

    [Fact]
    public async Task Get_ListsAvailablePlayersPerDay()
    {
        using var plain = CreateClientFor(PlainPlayerId);
        (await plain.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = true })).EnsureSuccessStatusCode();
        using var board = CreateClientFor(BoardPlayerId);

        var days = await board.GetFromJsonAsync<ToogAdminDay[]>("api/toog");

        Assert.NotNull(days);
        var day = Assert.Single(days, x => x.Date.Date == HomeDay.Date);
        Assert.Equal([PlainPlayerId], day.AvailablePlayerIds);
        Assert.Null(day.AssignedPlayerId);
    }

    [Fact]
    public async Task Assign_APlayerWhoDidNotVolunteer_CreatesAnAssignedRow()
    {
        using var board = CreateClientFor(BoardPlayerId);

        var response = await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = OtherPlayerId });

        response.EnsureSuccessStatusCode();
        await using var context = GetDbContext();
        var row = Assert.Single(await context.Toog.Where(x => x.Date == HomeDay.Date).ToArrayAsync());
        Assert.Equal(OtherPlayerId, row.PlayerId);
        Assert.True(row.Assigned);
    }

    [Fact]
    public async Task Assign_ReplacesThePreviousAssignmentOfThatDay()
    {
        using var board = CreateClientFor(BoardPlayerId);
        (await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = OtherPlayerId })).EnsureSuccessStatusCode();

        var response = await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = PlainPlayerId });

        response.EnsureSuccessStatusCode();
        await using var context = GetDbContext();
        var rows = await context.Toog.Where(x => x.Date == HomeDay.Date).ToArrayAsync();
        Assert.Equal(PlainPlayerId, Assert.Single(rows, x => x.Assigned).PlayerId);
    }

    [Fact]
    public async Task Assign_OfAVolunteer_KeepsHisRowAndDropsTheOtherAssignment()
    {
        using var plain = CreateClientFor(PlainPlayerId);
        (await plain.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = true })).EnsureSuccessStatusCode();
        using var board = CreateClientFor(BoardPlayerId);
        (await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = OtherPlayerId })).EnsureSuccessStatusCode();

        var response = await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = PlainPlayerId });

        response.EnsureSuccessStatusCode();
        await using var context = GetDbContext();
        var rows = await context.Toog.Where(x => x.Date == HomeDay.Date).ToArrayAsync();
        Assert.Equal(PlainPlayerId, Assert.Single(rows).PlayerId);
        Assert.True(rows[0].Assigned);
    }

    [Fact]
    public async Task Assign_WithoutPlayer_ClearsTheAssignmentButKeepsAVolunteerRow()
    {
        using var plain = CreateClientFor(PlainPlayerId);
        (await plain.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = true })).EnsureSuccessStatusCode();
        using var board = CreateClientFor(BoardPlayerId);
        (await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = PlainPlayerId })).EnsureSuccessStatusCode();

        var response = await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = null });

        response.EnsureSuccessStatusCode();
        await using var context = GetDbContext();
        var row = Assert.Single(await context.Toog.Where(x => x.Date == HomeDay.Date).ToArrayAsync());
        Assert.Equal(PlainPlayerId, row.PlayerId);
        Assert.False(row.Assigned);
    }

    [Fact]
    public async Task SetMine_Unavailable_WhileAssigned_IsForbidden()
    {
        using var plain = CreateClientFor(PlainPlayerId);
        (await plain.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = true })).EnsureSuccessStatusCode();
        using var board = CreateClientFor(BoardPlayerId);
        (await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = PlainPlayerId })).EnsureSuccessStatusCode();

        var response = await plain.PostAsJsonAsync("api/toog/mine", new ToogAvailabilityRequest { Date = HomeDay.Date, Available = false });

        Assert.Equal(HttpStatusCode.Forbidden, response.StatusCode);
        await using var context = GetDbContext();
        Assert.Single(await context.Toog.Where(x => x.Date == HomeDay.Date).ToArrayAsync());
    }

    [Fact]
    public async Task GetMine_AfterBeingAssigned_ReportsAssigned()
    {
        using var board = CreateClientFor(BoardPlayerId);
        (await board.PostAsJsonAsync("api/toog/assign", new ToogAssignRequest { Date = HomeDay.Date, PlayerId = PlainPlayerId })).EnsureSuccessStatusCode();
        using var plain = CreateClientFor(PlainPlayerId);

        var days = await plain.GetFromJsonAsync<ToogDay[]>("api/toog/mine");

        Assert.NotNull(days);
        var day = Assert.Single(days, x => x.Date.Date == HomeDay.Date);
        Assert.True(day.Available);
        Assert.True(day.Assigned);
    }
```

- [ ] **Step 2: Draai de tests en zie ze falen**

```bash
cd backend && dotnet test Ttc.slnx --filter FullyQualifiedName~ToogControllerTests
```

Verwacht: de 8 nieuwe tests falen (compileerfout of 403/404) — `api/toog` en `api/toog/assign`
bestaan nog niet.

- [ ] **Step 3: Vul de service aan**

Voeg toe aan `backend/src/Ttc.DataAccess/Services/ToogService.cs`:

```csharp
    public async Task<ToogAdminDay[]> Get()
    {
        var homeDays = await GetHomeDays();
        var rows = await _context.Toog
            .Where(x => x.Date >= TtcDbContext.GetCurrentBelgianDateTime().Date)
            .ToArrayAsync();

        return homeDays
            .Select(day => new ToogAdminDay
            {
                Date = day.Key,
                HomeTeamIds = day.Value,
                AvailablePlayerIds = rows.Where(x => x.Date == day.Key).Select(x => x.PlayerId).OrderBy(x => x).ToArray(),
                AssignedPlayerId = rows.FirstOrDefault(x => x.Date == day.Key && x.Assigned)?.PlayerId,
            })
            .ToArray();
    }

    public async Task<ToogAdminDay[]> Assign(DateTime date, int? playerId)
    {
        var day = date.Date;
        var rows = await _context.Toog.Where(x => x.Date == day).ToArrayAsync();

        foreach (var row in rows.Where(x => x.Assigned && x.PlayerId != playerId))
        {
            if (row.Volunteered)
            {
                row.Assigned = false;
            }
            else
            {
                _context.Toog.Remove(row);
            }
        }

        if (playerId.HasValue)
        {
            var target = rows.FirstOrDefault(x => x.PlayerId == playerId.Value);
            if (target == null)
            {
                _context.Toog.Add(new ToogEntity { Date = day, PlayerId = playerId.Value, Assigned = true });
            }
            else
            {
                target.Assigned = true;
            }
        }

        await _context.SaveChangesAsync();
        return await Get();
    }
```

Een rij die het bestuur zelf aanmaakte heeft `Volunteered = false` en verdwijnt daarom zodra de
aanduiding naar iemand anders gaat; de rij van een vrijwilliger blijft staan met `Assigned = false`.

- [ ] **Step 4: Voeg de admin-endpoints toe**

In `backend/src/Ttc.WebApi/Controllers/ToogController.cs`, onder `SetMine`:

```csharp
    [HttpGet]
    public async Task<ActionResult<ToogAdminDay[]>> Get()
    {
        if (!await _service.IsBoardMember())
        {
            return new StatusCodeResult(StatusCodes.Status403Forbidden);
        }

        return await _service.Get();
    }

    [HttpPost("assign")]
    public async Task<ActionResult<ToogAdminDay[]>> Assign([FromBody] ToogAssignRequest request)
    {
        if (!await _service.IsBoardMember())
        {
            return new StatusCodeResult(StatusCodes.Status403Forbidden);
        }

        return await _service.Assign(request.Date, request.PlayerId);
    }
```

- [ ] **Step 5: Draai alle toog-tests**

```bash
cd backend && dotnet test Ttc.slnx --filter FullyQualifiedName~ToogControllerTests
```

Verwacht: 12 passed.

- [ ] **Step 6: Draai de volledige backend-suite**

```bash
cd backend && dotnet test Ttc.slnx
```

Verwacht: alle tests groen.

- [ ] **Step 7: Commit**

```bash
git add backend/src
git commit -m "Let the board assign one bar duty volunteer per home day"
```

---

## Task 5: Toog-kolom in de Excel-export

**Files:**
- Modify: `backend/src/Ttc.DataAccess/Utilities/Excel/ExcelExportResources.resx`
- Modify: `backend/src/Ttc.DataAccess/Utilities/Excel/ExcelExportResources.Designer.cs`
- Modify: `backend/src/Ttc.DataAccess/Utilities/Excel/TeamsExcelCreator.cs`
- Modify: `backend/src/Ttc.DataAccess/Services/TeamService.cs`

- [ ] **Step 1: Voeg de resource toe**

In `ExcelExportResources.resx`, naast de bestaande `MatchBlock`-entry:

```xml
  <data name="MatchToog" xml:space="preserve">
    <value>Toog</value>
  </data>
```

In `ExcelExportResources.Designer.cs`, onder de `MatchBlock`-property:

```csharp
        /// <summary>
        ///   Looks up a localized string similar to Toog.
        /// </summary>
        internal static string MatchToog {
            get {
                return ResourceManager.GetString("MatchToog", resourceCulture);
            }
        }
```

- [ ] **Step 2: Breid het Excel-model uit**

In `TeamsExcelCreator.cs`, in `internal class TeamMatchExcelModel`, voeg een property toe:

```csharp
    public string? Toog { get; set; }
```

- [ ] **Step 3: Voeg de kolom toe aan de header**

In `SetHeader`, vervang:

```csharp
        headers.Add(ExcelExportResources.MatchBlock);
```

door:

```csharp
        headers.Add(ExcelExportResources.MatchBlock);
        headers.Add(ExcelExportResources.MatchToog);
```

`totalHeaderCount` telt automatisch mee, maar `Create()` gebruikt `totalColumnCount` als de
blok-kolom. Vervang daarom in `Create()`:

```csharp
                        sheet.Cells[rowIndex, totalColumnCount].Value = blockName;
```

door:

```csharp
                        sheet.Cells[rowIndex, totalColumnCount - 1].Value = blockName;
```

en voeg daaronder, buiten het `if (match.CaptainDecisions.Any())`-blok, toe:

```csharp
                    if (!string.IsNullOrEmpty(match.Toog))
                    {
                        sheet.Cells[rowIndex, totalColumnCount].Value = match.Toog;
                    }
```

- [ ] **Step 4: Vul de kolom in de model-builder**

Wijzig de signatuur van `CreateFormation` en `BuildFormationModel` in `TeamsExcelCreator.cs`:

```csharp
    public static TeamsExcelCreator CreateFormation(TeamEntity[] teams, List<MatchEntity> matches, PlayerEntity[] players, ClubEntity[] clubs, Dictionary<DateTime, string> toogPerDay)
        => new(BuildFormationModel(teams, matches, players, clubs, toogPerDay));

    internal static List<TeamExcelModel> BuildFormationModel(TeamEntity[] teams, List<MatchEntity> matches, PlayerEntity[] players, ClubEntity[] clubs, Dictionary<DateTime, string> toogPerDay)
```

En in de `foreach (var match in teamMatches)`-lus, vlak voor `teamModel.Matches.Add(teamMatch);`:

```csharp
                if (isHomeTeam && toogPerDay.TryGetValue(match.Date.Date, out string? toogName))
                {
                    teamMatch.Toog = toogName;
                }
```

- [ ] **Step 5: Geef de toog-toewijzingen mee**

In `backend/src/Ttc.DataAccess/Services/TeamService.cs`, in `GetExcelExport`, vlak voor de
`var excelCreator = ...`-regel:

```csharp
        var toogPerDay = await _context.Toog
            .Where(x => x.Assigned)
            .Include(x => x.Player)
            .ToDictionaryAsync(x => x.Date, x => x.Player!.Alias ?? x.Player.FirstName ?? "???");
```

en wijzig de aanroep naar:

```csharp
        var excelCreator = TeamsExcelCreator.CreateFormation(teams, matches, players, clubs, toogPerDay);
```

- [ ] **Step 6: Build en draai de backend-suite**

```bash
cd backend && dotnet build Ttc.slnx && dotnet test Ttc.slnx
```

Verwacht: `Build succeeded` met 0 warnings, alle tests groen.

- [ ] **Step 7: Commit**

```bash
git add backend/src
git commit -m "Show the assigned bar duty in the formation Excel export"
```

---

## Task 6: Frontend redux-slice

**Files:**
- Modify: `frontend/src/models/model-interfaces.ts`
- Create: `frontend/src/reducers/toogReducer.ts`
- Modify: `frontend/src/store.ts`
- Modify: `frontend/src/utils/test-utils.tsx`

- [ ] **Step 1: Voeg de interfaces toe**

Onderaan `frontend/src/models/model-interfaces.ts`:

```ts
export type IToogDay = {
  date: string;
  homeTeamIds: number[];
  available: boolean;
  assigned: boolean;
};

export type IToogAdminDay = {
  date: string;
  homeTeamIds: number[];
  availablePlayerIds: number[];
  assignedPlayerId: number | null;
};
```

- [ ] **Step 2: Schrijf de slice**

`frontend/src/reducers/toogReducer.ts`:

```ts
import { createAsyncThunk, createSlice } from '@reduxjs/toolkit';
import http from '../utils/httpClient';
import { showSnackbar } from './configReducer';
import { t } from '../locales';
import { IToogAdminDay, IToogDay } from '../models/model-interfaces';

type ToogState = {
  mine: IToogDay[];
  admin: IToogAdminDay[];
};

export const fetchMyToog = createAsyncThunk('toog/GetMine', async () => http.get<IToogDay[]>('/toog/mine'));

export const toggleMyToog = createAsyncThunk('toog/SetMine', async (params: { date: string; available: boolean }, { dispatch }) => {
  try {
    return await http.post<IToogDay[]>('/toog/mine', params);
  } catch (err) {
    dispatch(showSnackbar(t('common.apiFail')));
    throw err;
  }
});

export const fetchToogAdmin = createAsyncThunk('toog/Get', async () => http.get<IToogAdminDay[]>('/toog'));

export const assignToog = createAsyncThunk('toog/Assign', async (params: { date: string; playerId: number | null }, { dispatch }) => {
  try {
    return await http.post<IToogAdminDay[]>('/toog/assign', params);
  } catch (err) {
    dispatch(showSnackbar(t('common.apiFail')));
    throw err;
  }
});

const toogSlice = createSlice({
  name: 'toog',
  initialState: { mine: [], admin: [] } as ToogState,
  reducers: {},
  extraReducers: builder => {
    builder.addCase(fetchMyToog.fulfilled, (state, action) => ({ ...state, mine: action.payload }));
    builder.addCase(toggleMyToog.fulfilled, (state, action) => ({ ...state, mine: action.payload }));
    builder.addCase(fetchToogAdmin.fulfilled, (state, action) => ({ ...state, admin: action.payload }));
    builder.addCase(assignToog.fulfilled, (state, action) => ({ ...state, admin: action.payload }));
  },
});

export default toogSlice.reducer;
```

- [ ] **Step 3: Registreer de reducer**

In `frontend/src/store.ts`: `import toogReducer from './reducers/toogReducer';` en in het
`reducer`-object `toog: toogReducer,`.

In `frontend/src/utils/test-utils.tsx`: dezelfde import en `toog: toogReducer,` in
`rootReducer`.

- [ ] **Step 4: Typecheck en lint**

```bash
cd frontend && bun run build && bun run lint
```

Verwacht: build slaagt, 0 lint-warnings.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/models/model-interfaces.ts frontend/src/reducers/toogReducer.ts frontend/src/store.ts frontend/src/utils/test-utils.tsx
git commit -m "Add toog redux slice"
```

---

## Task 7: Toog-kolom in de ploegopstelling

**Files:**
- Modify: `frontend/src/utils/locales-nl.ts`
- Modify: `frontend/src/components/users/PlayerLineup.tsx`
- Modify: `frontend/src/components/users/Profile.tsx`
- Create: `frontend/src/components/users/spec/PlayerLineupToogSpec.tsx`

- [ ] **Step 1: Voeg de teksten toe**

In `frontend/src/utils/locales-nl.ts`, in het `profile.play`-blok, na `matchPlayed`:

```ts
        toogTitle: 'Toog',
        toogCanDo: 'Ik kan toog doen',
        toogAssigned: 'Neem contact op met het bestuur als je de toog toch niet kan doen',
        toogNoOwnMatch: 'Jouw ploeg speelt niet',
```

- [ ] **Step 2: Schrijf de falende test**

`frontend/src/components/users/spec/PlayerLineupToogSpec.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { screen, fireEvent, waitFor } from '@testing-library/react';
import dayjs from 'dayjs';
import { renderWithProviders } from '../../../utils/test-utils';
import PlayerLineup from '../PlayerLineup';
import TeamModel from '../../../models/TeamModel';
import http from '../../../utils/httpClient';

const playerId = 1;
const homeDay = dayjs().add(7, 'day').startOf('day');

const toogDays = [
  { date: homeDay.toISOString(), homeTeamIds: [10], available: false, assigned: false },
];

describe('PlayerLineup toog column', () => {
  beforeEach(() => {
    vi.spyOn(http, 'get').mockResolvedValue(toogDays);
    vi.spyOn(http, 'post').mockResolvedValue(toogDays.map(x => ({ ...x, available: true })));
  });

  it('shows a toog-only row for a home day without an own match', async () => {
    renderWithProviders(<PlayerLineup teams={[] as unknown as TeamModel[]} playerId={playerId} showToog />, {
      preloadedState: { toog: { mine: toogDays, admin: [] } },
    });

    expect(await screen.findByText('Ik kan toog doen')).toBeInTheDocument();
    expect(screen.getByText('Jouw ploeg speelt niet')).toBeInTheDocument();
  });

  it('posts the date when the toggle is clicked', async () => {
    renderWithProviders(<PlayerLineup teams={[] as unknown as TeamModel[]} playerId={playerId} showToog />, {
      preloadedState: { toog: { mine: toogDays, admin: [] } },
    });

    fireEvent.click(await screen.findByText('Ik kan toog doen'));

    await waitFor(() => expect(http.post).toHaveBeenCalledWith('/toog/mine', { date: homeDay.toISOString(), available: true }));
  });

  it('locks the toggle and explains who to contact once assigned', async () => {
    const assigned = [{ ...toogDays[0], available: true, assigned: true }];
    renderWithProviders(<PlayerLineup teams={[] as unknown as TeamModel[]} playerId={playerId} showToog />, {
      preloadedState: { toog: { mine: assigned, admin: [] } },
    });

    expect(await screen.findByText('Neem contact op met het bestuur als je de toog toch niet kan doen')).toBeInTheDocument();
    expect(screen.queryByText('Ik kan toog doen')).not.toBeInTheDocument();
  });

  it('renders one toggle when two own matches fall on the same home day', async () => {
    const team = new TeamModel({
      id: 10,
      competition: 'Vttl',
      teamCode: 'A',
      matches: [
        { id: 1, date: homeDay.hour(20).toISOString(), teamId: 10, players: [] },
        { id: 2, date: homeDay.hour(19).toISOString(), teamId: 10, players: [] },
      ],
    } as never);

    renderWithProviders(<PlayerLineup teams={[team]} playerId={playerId} showToog />, {
      preloadedState: { toog: { mine: toogDays, admin: [] } },
    });

    expect(await screen.findAllByText('Ik kan toog doen')).toHaveLength(1);
  });

  it('does not render the toog column without showToog', () => {
    renderWithProviders(<PlayerLineup teams={[] as unknown as TeamModel[]} playerId={playerId} />, {
      preloadedState: { toog: { mine: toogDays, admin: [] } },
    });

    expect(screen.queryByText('Toog')).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Draai de test en zie ze falen**

```bash
cd frontend && bun run test -- PlayerLineupToogSpec
```

Verwacht: falen — `showToog` bestaat niet en er is geen toog-kolom.

- [ ] **Step 4: Bouw de toog-kolom**

In `frontend/src/components/users/PlayerLineup.tsx`:

Voeg toe aan de imports. `useState` wordt al uit `react` gehaald en `useTtcDispatch` al uit
`storeHooks`, dus vul die bestaande regels aan in plaats van ze te dupliceren:

```tsx
import React, { useEffect, useState } from 'react';
import { IMatch, ITeam, IToogDay, MatchPlayerStatus } from '../../models/model-interfaces';
import { useTtcDispatch, useTtcSelector } from '../../utils/hooks/storeHooks';
import { fetchMyToog, toggleMyToog } from '../../reducers/toogReducer';
```

Breid de props uit:

```tsx
type PlayerLineupProps = {
  playerId: number;
  teams: ITeam[];
  disableBlockedMatches?: boolean;
  /** Toog-endpoints werken altijd op de ingelogde speler, dus niet in de kapiteinsweergave */
  showToog?: boolean;
};
```

Binnen de component, na de bestaande `useState`-regels:

```tsx
  const toogDays = useTtcSelector(state => state.toog.mine);

  useEffect(() => {
    if (showToog) {
      dispatch(fetchMyToog());
    }
  }, [dispatch, showToog]);
```

Bouw de rijen. Vervang de bestaande `const { matches, hasMore } = ...` door:

```tsx
  const { matches, hasMore } = getFirstOrLastMatches(allMatchesToCome, matchesFilter);

  const toogRows = showToog ? toogDays.filter(day => !matches.some(match => dayjs(match.date).isSame(day.date, 'day'))) : [];
  const rows: ({ match: IMatch; toog?: IToogDay } | { match?: undefined; toog: IToogDay })[] = [
    ...matches.map(match => ({ match, toog: showToog ? toogDays.find(day => dayjs(match.date).isSame(day.date, 'day')) : undefined })),
    ...toogRows.map(toog => ({ toog })),
  ].sort((a, b) => dayjs(a.match?.date ?? a.toog!.date).valueOf() - dayjs(b.match?.date ?? b.toog!.date).valueOf());
```

Een toog-toggle staat op de eerste rij van die dag; latere rijen van dezelfde dag krijgen er geen.
Filter dat direct na het sorteren:

```tsx
  const seenToogDates = new Set<string>();
  const rowsWithToog = rows.map(row => {
    if (!row.toog || seenToogDates.has(row.toog.date)) {
      return { ...row, toog: undefined };
    }
    seenToogDates.add(row.toog.date);
    return row;
  });
```

Voeg de kolomkop toe in de `<thead>`:

```tsx
            {showToog ? <th>{t('profile.play.toogTitle')}</th> : null}
```

Vervang `{matches.map(match => {` door `{rowsWithToog.map(row => {`, en bovenaan die
callback:

```tsx
            const { match, toog } = row;
            const rowKey = match ? `match-${match.id}` : `toog-${toog!.date}`;

            const toogCell = !showToog ? null : (
              <td>
                {toog?.assigned ? (
                  <div className="text-muted">
                    <Icon fa="fa fa-lock" style={{ marginRight: 4 }} />
                    {t('profile.play.toogAssigned')}
                  </div>
                ) : toog ? (
                  <Button
                    variant={toog.available ? 'success' : 'outline-secondary'}
                    onClick={() => dispatch(toggleMyToog({ date: toog.date, available: !toog.available }))}
                  >
                    {t('profile.play.toogCanDo')}
                  </Button>
                ) : null}
              </td>
            );

            if (!match) {
              return (
                <tr key={rowKey}>
                  <td className="d-none d-lg-table-cell" />
                  <td className="d-none d-sm-table-cell">{t('match.date', dayjs(toog!.date).format('ddd D/M'))}</td>
                  <td className="text-muted">{t('profile.play.toogNoOwnMatch')}</td>
                  <td />
                  <td />
                  {toogCell}
                </tr>
              );
            }
```

Laat de rest van de bestaande callback ongewijzigd, maar hang `{toogCell}` achteraan in de
bestaande `<tr>`, na de laatste `<td>`, en vervang `key={match.id}` door `key={rowKey}`.

- [ ] **Step 5: Zet de prop aan in het profiel**

In `frontend/src/components/users/Profile.tsx`, regel 52:

```tsx
        return <PlayerLineup teams={yourTeams} playerId={user.playerId} disableBlockedMatches showToog />;
```

- [ ] **Step 6: Draai de tests**

```bash
cd frontend && bun run test -- PlayerLineupToogSpec
```

Verwacht: 5 passed.

- [ ] **Step 7: Draai de hele frontend-suite en de lint**

```bash
cd frontend && bun run test && bun run lint && bun run build
```

Verwacht: alles groen, 0 warnings.

- [ ] **Step 8: Commit**

```bash
git add frontend/src
git commit -m "Let a player volunteer for bar duty from their lineup page"
```

---

## Task 8: Admin-tab "Toog"

**Files:**
- Create: `frontend/src/components/admin/AdminToog.tsx`
- Modify: `frontend/src/components/admin/Admin.tsx`
- Create: `frontend/src/components/admin/spec/AdminToogSpec.tsx`

- [ ] **Step 1: Schrijf de falende test**

`frontend/src/components/admin/spec/AdminToogSpec.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { screen, fireEvent, waitFor } from '@testing-library/react';
import dayjs from 'dayjs';
import { renderWithProviders } from '../../../utils/test-utils';
import { AdminToog } from '../AdminToog';
import http from '../../../utils/httpClient';

const homeDay = dayjs().add(7, 'day').startOf('day');

const adminDays = [
  { date: homeDay.toISOString(), homeTeamIds: [10], availablePlayerIds: [1], assignedPlayerId: null },
];

const preloadedState = {
  toog: { mine: [], admin: adminDays },
  players: [{ id: 1, alias: 'Wouter' }],
};

describe('AdminToog', () => {
  beforeEach(() => {
    vi.spyOn(http, 'get').mockResolvedValue(adminDays);
    vi.spyOn(http, 'post').mockResolvedValue(adminDays.map(x => ({ ...x, assignedPlayerId: 1 })));
  });

  it('shows a button per available player', async () => {
    renderWithProviders(<AdminToog />, { preloadedState: preloadedState as never });

    expect(await screen.findByRole('button', { name: 'Wouter' })).toBeInTheDocument();
  });

  it('assigns the player when their button is clicked', async () => {
    renderWithProviders(<AdminToog />, { preloadedState: preloadedState as never });

    fireEvent.click(await screen.findByRole('button', { name: 'Wouter' }));

    await waitFor(() => expect(http.post).toHaveBeenCalledWith('/toog/assign', { date: homeDay.toISOString(), playerId: 1 }));
  });

  it('clears the assignment', async () => {
    const assigned = [{ ...adminDays[0], assignedPlayerId: 1 }];
    renderWithProviders(<AdminToog />, { preloadedState: { ...preloadedState, toog: { mine: [], admin: assigned } } as never });

    fireEvent.click(await screen.findByLabelText('Aanduiding wissen'));

    await waitFor(() => expect(http.post).toHaveBeenCalledWith('/toog/assign', { date: homeDay.toISOString(), playerId: null }));
  });
});
```

- [ ] **Step 2: Draai de test en zie ze falen**

```bash
cd frontend && bun run test -- AdminToogSpec
```

Verwacht: falen — `AdminToog` bestaat niet.

- [ ] **Step 3: Schrijf de component**

`frontend/src/components/admin/AdminToog.tsx`:

```tsx
import { useEffect } from 'react';
import dayjs from 'dayjs';
import Table from 'react-bootstrap/Table';
import Button from 'react-bootstrap/Button';
import { PlayerAutoComplete } from '../players/PlayerAutoComplete';
import { Icon } from '../controls/Icons/Icon';
import { t } from '../../locales';
import { assignToog, fetchToogAdmin } from '../../reducers/toogReducer';
import { selectPlayers, useTtcDispatch, useTtcSelector } from '../../utils/hooks/storeHooks';

export const AdminToog = () => {
  const dispatch = useTtcDispatch();
  const days = useTtcSelector(state => state.toog.admin);
  const players = useTtcSelector(selectPlayers);

  useEffect(() => {
    dispatch(fetchToogAdmin());
  }, [dispatch]);

  const playerName = (playerId: number) => players.find(ply => ply.id === playerId)?.alias ?? `#${playerId}`;

  return (
    <Table size="sm">
      <thead>
        <tr>
          <th>{t('common.date')}</th>
          <th>{t('admin.toog.volunteers')}</th>
          <th>{t('admin.toog.other')}</th>
        </tr>
      </thead>
      <tbody>
        {days.map(day => (
          <tr key={day.date} className={day.assignedPlayerId ? undefined : 'table-warning'}>
            <td>{dayjs(day.date).format('dd DD/MM')}</td>
            <td>
              {day.availablePlayerIds.map(availableId => (
                <Button
                  key={availableId}
                  size="sm"
                  style={{ marginRight: 5 }}
                  variant={day.assignedPlayerId === availableId ? 'success' : 'outline-secondary'}
                  onClick={() => dispatch(assignToog({ date: day.date, playerId: availableId }))}
                >
                  {playerName(availableId)}
                </Button>
              ))}
              {day.assignedPlayerId && !day.availablePlayerIds.includes(day.assignedPlayerId) ? (
                <Button size="sm" variant="success" style={{ marginRight: 5 }}>
                  {playerName(day.assignedPlayerId)}
                </Button>
              ) : null}
              {day.assignedPlayerId ? (
                <Button size="sm" variant="link" aria-label={t('admin.toog.clear')} onClick={() => dispatch(assignToog({ date: day.date, playerId: null }))}>
                  <Icon fa="fa fa-times" />
                </Button>
              ) : null}
            </td>
            <td>
              <PlayerAutoComplete selectPlayer={playerId => dispatch(assignToog({ date: day.date, playerId }))} label={t('admin.toog.other')} />
            </td>
          </tr>
        ))}
      </tbody>
    </Table>
  );
};
```

- [ ] **Step 4: Voeg de teksten toe**

In `frontend/src/utils/locales-nl.ts` bestaat al een `admin`-blok met een `board`-sleutel
(rond regel 480). Voeg daar `toog` naast:

```ts
      toog: {
        volunteers: 'Gaf zich op',
        other: 'Andere speler',
        clear: 'Aanduiding wissen',
      },
```

- [ ] **Step 5: Voeg de tab toe**

In `frontend/src/components/admin/Admin.tsx`:

- import: `import { AdminToog } from './AdminToog';`
- in `tabEventKeys`: `toog: 'toog',`
- in `renderSection`: `case tabEventKeys.toog: return <AdminToog />;`
- in `tabConfig`, na de `formation`-regel: `{ key: tabEventKeys.toog, title: 'Toog' },`

- [ ] **Step 6: Draai de tests**

```bash
cd frontend && bun run test -- AdminToogSpec
```

Verwacht: 3 passed.

- [ ] **Step 7: Draai alles**

```bash
cd frontend && bun run test && bun run lint && bun run build && bun run knip
```

Verwacht: alles groen, 0 warnings, geen dead code.

- [ ] **Step 8: Commit**

```bash
git add frontend/src
git commit -m "Add admin tab to assign bar duty per home day"
```

---

## Task 9: Eindcontrole

- [ ] **Step 1: Volledige backend-suite**

```bash
cd backend && dotnet build Ttc.slnx && dotnet test Ttc.slnx
```

Verwacht: `Build succeeded` met 0 warnings, alle tests groen.

- [ ] **Step 2: Volledige frontend-suite**

```bash
cd frontend && bun run lint && bun run test && bun run build && bun run knip
```

Verwacht: 0 lint-warnings, alle tests groen, build slaagt, knip meldt niets nieuws.

- [ ] **Step 3: Handmatige rooktest**

```bash
docker compose -f docker-compose.dev.yml up
```

- `http://localhost:3000/profiel/ploegopstelling` → toog-kolom, een toog-only rij voor een
  thuisdag zonder eigen match, toggle bewaart na refresh.
- `http://localhost:3000/admin/toog` → de opgegeven speler staat er als knop; aanduiden kleurt
  groen; de speler ziet op zijn eigen pagina het slot met de bestuursmelding.
- `http://localhost:3000` → ploegopstellingen-Excel downloaden en de kolom "Toog" controleren.
